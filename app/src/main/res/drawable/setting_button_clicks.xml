<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_pressed="true"
        android:drawable="@color/pressColor"/> <!-- pressed state -->
    <item android:state_focused="true"
        android:drawable="@color/dimBlackColorBg"/> <!-- focused state -->
    <item   android:drawable="@color/dimBlackColorBg"/>
</selector>